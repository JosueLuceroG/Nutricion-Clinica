diff --git a/src/app/pages/dashboard/DashboardHeader.tsx b/src/app/pages/dashboard/DashboardHeader.tsx
index ce75d4b..be78526 100644
--- a/src/app/pages/dashboard/DashboardHeader.tsx
+++ b/src/app/pages/dashboard/DashboardHeader.tsx
@@ -42,6 +42,19 @@ function getGreetingForHour(date = new Date()): string {
   return "Buenas noches";
 }
 
+function getGreetingEmoji(date = new Date()): string {
+  const hour = date.getHours();
+  const startOfYear = Date.UTC(date.getFullYear(), 0, 0);
+  const dayOfYear = Math.floor((Date.UTC(date.getFullYear(), date.getMonth(), date.getDate()) - startOfYear) / 86_400_000);
+  const emojis = hour >= 5 && hour < 12
+    ? ["👋", "☀️", "🌱", "🍎"]
+    : hour >= 12 && hour < 19
+      ? ["🌿", "🥗", "💙", "✨"]
+      : ["👋", "💙", "🩺", "🌱"];
+
+  return emojis[dayOfYear % emojis.length] ?? "👋";
+}
+
 function getInitials(fullName: string): string {
   return fullName
     .split(/\s+/)
@@ -62,7 +75,9 @@ export function DashboardHeader({ onCustomizeKpis }: DashboardHeaderProps) {
   const unread = useNotificationStore((state) => state.unread);
   const displayName = user?.nombreCompleto?.trim() || "Administrador";
   const firstName = getFirstName(displayName);
-  const greeting = getGreetingForHour();
+  const now = new Date();
+  const greeting = getGreetingForHour(now);
+  const greetingEmoji = getGreetingEmoji(now);
   const initials = getInitials(displayName);
   const notificationCount = unread > 0 ? unread : 0;
   const hasUnreadNotifications = notificationCount > 0;
@@ -114,15 +129,9 @@ export function DashboardHeader({ onCustomizeKpis }: DashboardHeaderProps) {
         <section className="nc-dashboard-header__intro" aria-label="Resumen del día">
           <h1 className="nc-dashboard-header__title">
             <span className="nc-dashboard-header__greetingText">{greeting}, {firstName}</span>
-            <span className="nc-dashboard-header__greetingEmoji" aria-hidden="true">👋</span>
+            <span className="nc-dashboard-header__greetingEmoji" aria-hidden="true">{greetingEmoji}</span>
           </h1>
           <p className="nc-dashboard-header__subtitle">Aquí tienes el resumen de tu clínica hoy.</p>
-          {onCustomizeKpis && (
-            <button type="button" className="nc-dashboard-header__metricsAction" onClick={onCustomizeKpis}>
-              <SlidersHorizontal size={14} strokeWidth={2} aria-hidden="true" />
-              <span>Reordenar / ocultar métricas</span>
-            </button>
-          )}
         </section>
 
         <div className="nc-dashboard-header__searchSlot">
@@ -255,6 +264,13 @@ export function DashboardHeader({ onCustomizeKpis }: DashboardHeaderProps) {
             </DropdownMenu>
           </div>
         </div>
+
+        {onCustomizeKpis && (
+          <button type="button" className="nc-dashboard-header__metricsAction" onClick={onCustomizeKpis}>
+            <SlidersHorizontal size={14} strokeWidth={2} aria-hidden="true" />
+            <span>Reordenar / ocultar métricas</span>
+          </button>
+        )}
       </div>
     </header>
   );
diff --git a/src/app/pages/dashboard/DashboardPage.css b/src/app/pages/dashboard/DashboardPage.css
index c5e9f1d..6b392f3 100644
--- a/src/app/pages/dashboard/DashboardPage.css
+++ b/src/app/pages/dashboard/DashboardPage.css
@@ -41,17 +41,27 @@
   --nc-dashboard-card-pad: var(--nc-card-pad);
   color-scheme: light;
   display: grid;
+  width: 100%;
+  max-width: 100vw;
   grid-template-columns: var(--nc-dashboard-sidebar-w) minmax(0, 1fr);
   min-height: 100%;
   height: 100%;
-  overflow: hidden;
+  overflow: clip;
   background:
     radial-gradient(circle at 45% 0%, rgba(37, 99, 235, 0.02), transparent 34%),
     radial-gradient(circle at 92% 12%, rgba(20, 184, 166, 0.018), transparent 32%),
     linear-gradient(180deg, #ffffff 0%, #ffffff 44%, #fbfdff 100%);
   color: #0b1745;
+  box-sizing: border-box;
   font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
   grid-template-rows: minmax(0, 1fr);
+  transition: grid-template-columns 180ms ease;
+}
+
+.nc-dashboard-shell *,
+.nc-dashboard-shell *::before,
+.nc-dashboard-shell *::after {
+  box-sizing: inherit;
 }
 
 .nc-dashboard-shell.nc-dashboard-shell--collapsed {
@@ -59,7 +69,7 @@
 }
 
 .nc-dashboard-shell--tauri {
-  grid-template-rows: 29px minmax(0, 1fr);
+  grid-template-rows: 28px minmax(0, 1fr);
   background: #ffffff;
 }
 
@@ -73,10 +83,10 @@
   grid-row: 1;
   display: flex;
   min-width: 0;
-  height: 29px;
+  height: 28px;
   align-items: center;
   justify-content: space-between;
-  border-bottom: 1px solid rgba(224, 231, 242, 0.62);
+  border-bottom: 1px solid rgba(224, 231, 242, 0.52);
   background: rgba(255, 255, 255, 0.992);
   color: #16234f;
   box-shadow: inset 0 -1px 0 rgba(255, 255, 255, 0.9);
@@ -88,12 +98,12 @@
   display: flex;
   min-width: 0;
   align-items: center;
-  gap: 4px;
+  gap: 3.5px;
   height: 100%;
-  padding-left: 13px;
-  font-size: 10.9px;
+  padding-left: 12px;
+  font-size: 10.7px;
   font-weight: 700;
-  letter-spacing: -0.022em;
+  letter-spacing: -0.024em;
 }
 
 .nc-dashboard-desktop-titlebar__brand img {
@@ -122,8 +132,8 @@
 .nc-dashboard-desktop-titlebar__subtitle {
   min-width: 0;
   overflow: hidden;
-  color: #596781;
-  font-size: 9.6px;
+  color: #647089;
+  font-size: 9.4px;
   font-weight: 600;
   letter-spacing: -0.012em;
   line-height: 1;
@@ -133,21 +143,21 @@
 
 .nc-dashboard-desktop-titlebar__desktopIndicator {
   display: inline-flex;
-  width: 18px;
-  height: 18px;
+  width: 15px;
+  height: 15px;
   flex: 0 0 auto;
   align-items: center;
   justify-content: center;
-  border: 1px solid rgba(200, 215, 240, 0.78);
-  border-radius: 7px;
-  background: rgba(250, 252, 255, 0.86);
-  color: #71809a;
-  margin-left: 3px;
+  border: 1px solid rgba(205, 216, 234, 0.38);
+  border-radius: 5px;
+  background: rgba(250, 252, 255, 0.34);
+  color: #858fa2;
+  margin-left: 2px;
 }
 
 .nc-dashboard-desktop-titlebar__desktopIndicator svg {
-  width: 10.5px;
-  height: 10.5px;
+  width: 9px;
+  height: 9px;
 }
 
 .nc-dashboard-desktop-titlebar__controls {
@@ -159,7 +169,7 @@
 
 .nc-dashboard-desktop-titlebar__controls button {
   display: inline-flex;
-  width: 38px;
+  width: 36px;
   align-items: center;
   justify-content: center;
   border: 0;
@@ -287,7 +297,7 @@
   position: relative;
   background: linear-gradient(135deg, #367dff 0%, #1f61f2 100%);
   color: #ffffff;
-  box-shadow: 0 11px 24px rgba(37, 99, 235, 0.16), inset 0 1px 0 rgba(255, 255, 255, 0.14);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.14);
   font-weight: 580;
   transform: translateX(0);
 }
@@ -671,7 +681,7 @@
 .nc-dashboard-shell--collapsed .nc-dashboard-sidebar__item--active:focus-visible {
   background: linear-gradient(135deg, #2f7bff 0%, #1f5bff 100%);
   color: #ffffff;
-  box-shadow: 0 11px 22px rgba(37, 99, 235, 0.23);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.14);
   transform: translateY(0);
 }
 
@@ -695,11 +705,13 @@
 
 .nc-dashboard-workspace {
   display: grid;
+  width: 100%;
+  max-width: 100%;
   grid-template-rows: auto minmax(0, 1fr) auto;
   min-width: 0;
   min-height: 0;
   height: 100%;
-  overflow: hidden;
+  overflow: clip;
 }
 
 .nc-dashboard-mobile-nav {
@@ -716,6 +728,9 @@
 }
 
 .nc-dashboard-header {
+  width: 100%;
+  max-width: 100%;
+  min-width: 0;
   min-height: var(--nc-header-h);
   margin: 0;
   border-width: 0 0 1px;
@@ -730,13 +745,20 @@
 .nc-dashboard-header__inner {
   display: grid;
   width: min(100%, var(--nc-dashboard-header-w));
+  max-width: 100%;
+  min-width: 0;
   margin: 0 auto;
-  grid-template-columns: minmax(260px, 0.64fr) minmax(300px, 430px) max-content;
+  grid-template-areas:
+    "intro search actions"
+    "metrics metrics metrics";
+  grid-template-columns: minmax(250px, 300px) minmax(300px, 1fr) max-content;
   align-items: center;
-  gap: var(--nc-dashboard-header-gap);
+  column-gap: var(--nc-dashboard-header-gap);
+  row-gap: 10px;
 }
 
 .nc-dashboard-header__intro {
+  grid-area: intro;
   display: grid;
   gap: 4px;
   min-width: 0;
@@ -780,10 +802,14 @@
 }
 
 .nc-dashboard-header__metricsAction {
+  grid-area: metrics;
+  justify-self: start;
   display: inline-flex;
+  width: max-content;
+  max-width: 100%;
   align-items: center;
   gap: 7px;
-  margin-top: clamp(11px, calc(13px * var(--nc-font-scale)), 16px);
+  margin: 0;
   border: 0;
   background: transparent;
   color: #1265ff;
@@ -800,15 +826,20 @@
 }
 
 .nc-dashboard-header__searchSlot {
+  grid-area: search;
   display: flex;
+  width: 100%;
+  max-width: 520px;
   min-width: 0;
   align-items: center;
+  justify-self: center;
 }
 
 .nc-dashboard-search {
   display: flex;
   height: clamp(38px, calc(42px * var(--nc-font-scale)), 46px);
   width: 100%;
+  max-width: 100%;
   min-width: 0;
   align-items: center;
   gap: 12px;
@@ -871,10 +902,13 @@
 }
 
 .nc-dashboard-header__actions {
+  grid-area: actions;
   display: flex;
+  max-width: 100%;
   min-width: 0;
   align-items: center;
   justify-content: flex-end;
+  justify-self: end;
   gap: 10px;
 }
 
@@ -952,11 +986,11 @@
 .nc-dashboard-button--primary {
   background: linear-gradient(135deg, #2f78ff 0%, #195de8 100%);
   color: #ffffff;
-  box-shadow: 0 16px 34px rgba(37, 99, 235, 0.24);
+  box-shadow: none;
 }
 
 .nc-dashboard-button--primary:hover {
-  box-shadow: 0 18px 34px rgba(37, 99, 235, 0.31);
+  box-shadow: none;
 }
 
 .nc-dashboard-header__notification {
@@ -1407,8 +1441,11 @@
 }
 
 .nc-dashboard-main {
+  width: 100%;
+  max-width: 100%;
   min-width: 0;
   min-height: 0;
+  overflow-x: clip;
   overflow-y: auto;
   padding: 0 var(--nc-dashboard-main-x) var(--nc-dashboard-main-bottom);
   scrollbar-color: rgba(143, 157, 186, 0.55) transparent;
@@ -1439,6 +1476,8 @@
 .nc-dashboard-container {
   display: grid;
   width: min(100%, var(--nc-dashboard-container-w));
+  max-width: 100%;
+  min-width: 0;
   margin: 0 auto;
   gap: var(--nc-dashboard-container-gap);
   padding: var(--nc-dashboard-container-top) 0 var(--nc-dashboard-container-bottom);
@@ -1455,9 +1494,9 @@
 
 @media (min-width: 768px) {
   .nc-dashboard-kpi-grid {
-    margin: -10px -10px -2px;
+    margin: -10px 0 -2px;
     border-radius: calc(var(--nc-dashboard-card-radius) + 14px);
-    padding: 10px;
+    padding: 10px 0;
   }
 
   .nc-dashboard-kpi-grid::before {
@@ -2751,6 +2790,9 @@
 }
 
 .nc-dashboard-bottom-frame {
+  width: 100%;
+  max-width: 100%;
+  min-width: 0;
   min-height: 38px;
   border-width: 1px 0 0;
   border-color: rgba(231, 237, 247, 0.9);
@@ -2762,6 +2804,8 @@
 
 .nc-dashboard-bottom-bar {
   display: flex;
+  width: 100%;
+  max-width: 100%;
   min-width: 0;
   align-items: center;
   gap: 10px;
@@ -2863,7 +2907,7 @@
 }
 
 .nc-dashboard-bottom-bar__spacer {
-  min-width: 12px;
+  min-width: 0;
   flex: 1 1 auto;
 }
 
@@ -3050,7 +3094,7 @@
   }
 
   .nc-dashboard-header__metricsAction {
-    margin-top: 9px;
+    margin-top: 0;
     font-size: 12px;
   }
 
@@ -3440,7 +3484,7 @@
   }
 
   .nc-dashboard-header__metricsAction {
-    margin-top: 10px;
+    margin-top: 0;
     font-size: 12px;
   }
 
@@ -5322,11 +5366,15 @@
 }
 
 .nc-dashboard-shell--tauri {
-  --nc-impact-h: 216px;
+  --nc-header-max-w: 100%;
+  --nc-container-max-w: 100%;
+  --nc-main-pad-x: 32px;
+  --nc-dashboard-main-x: 32px;
+  --nc-impact-h: 232px;
   --nc-impact-pad-x: 19px;
-  --nc-impact-pad-y: 19px;
-  --nc-impact-plant-w: 94px;
-  --nc-impact-plant-h: 144px;
+  --nc-impact-pad-y: 21px;
+  --nc-impact-plant-w: 102px;
+  --nc-impact-plant-h: 154px;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-header {
@@ -5334,14 +5382,16 @@
   background: #ffffff;
   box-shadow: none;
   min-height: 106px;
-  padding-top: max(11px, var(--nc-main-pad-y));
+  padding-top: max(8px, var(--nc-main-pad-y));
   padding-bottom: 7px;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-header__inner {
   align-items: center;
-  grid-template-columns: minmax(298px, 0.78fr) minmax(300px, 380px) max-content;
-  gap: 18px;
+  grid-template-columns: minmax(286px, 410px) minmax(250px, 456px) max-content;
+  justify-content: space-between;
+  column-gap: clamp(10px, 0.95vw, 18px);
+  row-gap: 14px;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-header__intro {
@@ -5350,7 +5400,7 @@
 
 .nc-dashboard-shell--tauri .nc-dashboard-header__title {
   color: #0b1745;
-  font-size: clamp(18px, calc(18.8px * var(--nc-header-font-scale, var(--nc-font-scale))), 19.6px);
+  font-size: clamp(18px, calc(18.6px * var(--nc-header-font-scale, var(--nc-font-scale))), 19.4px);
   font-weight: 620;
   letter-spacing: -0.035em;
   line-height: 1.16;
@@ -5364,20 +5414,22 @@
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-header__metricsAction {
-  margin-top: 9px;
+  margin-top: 0;
+  margin-bottom: 0;
   color: #1767ff;
-  font-size: 11.8px;
-  font-weight: 600;
+  font-size: 12.2px;
+  font-weight: 610;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-search {
-  height: 39px;
-  border-radius: 11px;
-  border-color: rgba(222, 230, 244, 0.62);
+  height: 38px;
+  gap: 10px;
+  border-radius: 12px;
+  border-color: rgba(216, 226, 244, 0.82);
   background: #ffffff;
-  box-shadow: 0 6px 16px rgba(31, 78, 150, 0.022);
+  box-shadow: 0 7px 18px rgba(31, 78, 150, 0.022);
   padding-right: 11px;
-  padding-left: 12px;
+  padding-left: 13px;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-search:hover {
@@ -5386,19 +5438,22 @@
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-search__placeholder {
-  font-size: 12.5px;
+  font-size: 12.6px;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-search__kbd {
-  height: 21px;
-  min-width: 40px;
+  height: 20px;
+  min-width: 39px;
   border-radius: 6px;
-  font-size: 10.6px;
+  border-color: rgba(219, 227, 241, 0.82);
+  background: #f5f8fd;
+  font-size: 10.3px;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-header__actions {
   align-items: center;
-  gap: 10px;
+  gap: 8px;
+  margin-right: 0;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-header__ctaGroup {
@@ -5409,11 +5464,23 @@
   gap: 8px;
 }
 
+.nc-dashboard-shell--tauri .nc-dashboard-button {
+  height: 39px;
+  border-radius: 11px;
+  padding-right: 13px;
+  padding-left: 13px;
+}
+
 .nc-dashboard-shell--tauri .nc-dashboard-button--outline,
 .nc-dashboard-shell--tauri .nc-dashboard-button--soft {
-  border-color: rgba(222, 230, 244, 0.62);
+  border-color: rgba(222, 230, 244, 0.66);
   background: #ffffff;
-  box-shadow: 0 6px 16px rgba(31, 78, 150, 0.022);
+  box-shadow: none;
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-button--outline:hover,
+.nc-dashboard-shell--tauri .nc-dashboard-button--soft:hover {
+  box-shadow: none;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-button--outline {
@@ -5421,28 +5488,74 @@
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-button--primary {
-  box-shadow: 0 11px 24px rgba(37, 99, 235, 0.18);
+  padding-right: 15px;
+  padding-left: 15px;
+  box-shadow: none;
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-button--primary:hover {
+  box-shadow: none;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-header__notification {
-  width: 39px;
-  height: 39px;
+  width: 38px;
+  height: 38px;
   border-radius: 12px;
+  color: #243156;
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-header__notification:hover {
+  background: rgba(37, 99, 235, 0.045);
+  color: #145fff;
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-header__badge {
+  right: -2px;
+  top: -4px;
+  min-width: 17px;
+  height: 17px;
+  border-width: 2px;
+  font-size: 9.5px;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-header__avatar,
 .nc-dashboard-shell--tauri .nc-dashboard-header__avatarFallback {
-  width: 41px;
-  height: 41px;
+  width: 40px;
+  height: 40px;
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-header__avatarFallback {
+  border-width: 2px;
+  box-shadow: 0 7px 16px rgba(20, 34, 74, 0.09);
+  font-size: 12.6px;
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-header__avatarStatus {
+  right: -1px;
+  bottom: 2px;
+  width: 11px;
+  height: 11px;
+  border-width: 2px;
+  box-shadow: 0 0 0 2px rgba(34, 197, 94, 0.12);
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-container {
-  padding-top: 8px;
+  width: 100%;
+  padding-top: 5px;
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-bottom-bar {
+  padding-right: var(--nc-dashboard-main-x);
+  padding-left: var(--nc-dashboard-main-x);
+}
+
+.nc-dashboard-shell--tauri.nc-dashboard-shell--collapsed .nc-dashboard-header__inner {
+  grid-template-columns: minmax(360px, 430px) minmax(440px, 496px) max-content;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-kpi-grid {
   gap: max(10px, calc(var(--nc-gap) - 5px));
-  margin-top: -8px;
+  margin-top: -2px;
   margin-bottom: -4px;
 }
 
@@ -5453,11 +5566,11 @@
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-kpi-card {
-  border-color: rgba(222, 231, 246, 0.68);
+  border-color: rgba(220, 230, 247, 0.74);
   background: #ffffff;
   box-shadow:
-    0 10px 26px rgba(31, 78, 150, 0.04),
-    0 2px 8px rgba(37, 99, 235, 0.018),
+    0 16px 38px rgba(31, 78, 150, 0.058),
+    0 4px 16px rgba(37, 99, 235, 0.028),
     inset 0 1px 0 rgba(255, 255, 255, 0.94);
 }
 
@@ -5468,15 +5581,15 @@
 .nc-dashboard-shell--tauri .nc-dashboard-kpi-card:hover {
   border-color: rgba(37, 99, 235, 0.16);
   box-shadow:
-    0 12px 30px rgba(31, 78, 150, 0.052),
-    0 3px 10px rgba(37, 99, 235, 0.026),
+    0 18px 42px rgba(31, 78, 150, 0.068),
+    0 5px 18px rgba(37, 99, 235, 0.034),
     inset 0 1px 0 rgba(255, 255, 255, 0.96);
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-section-card {
-  border-color: rgba(224, 232, 246, 0.54);
+  border-color: rgba(224, 232, 246, 0.58);
   background: #ffffff;
-  box-shadow: 0 5px 16px rgba(31, 78, 150, 0.018), inset 0 1px 0 rgba(255, 255, 255, 0.94);
+  box-shadow: 0 8px 22px rgba(31, 78, 150, 0.024), inset 0 1px 0 rgba(255, 255, 255, 0.94);
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-section-card::before {
@@ -5487,7 +5600,7 @@
 .nc-dashboard-shell--tauri .nc-dashboard-section-card--financial,
 .nc-dashboard-shell--tauri .nc-dashboard-section-card--payments,
 .nc-dashboard-shell--tauri .nc-dashboard-section-card--actions {
-  box-shadow: 0 5px 14px rgba(31, 78, 150, 0.016), inset 0 1px 0 rgba(255, 255, 255, 0.94);
+  box-shadow: 0 7px 20px rgba(31, 78, 150, 0.022), inset 0 1px 0 rgba(255, 255, 255, 0.94);
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-sidebar {
@@ -5498,53 +5611,81 @@
 .nc-dashboard-shell--tauri .nc-dashboard-sidebar__item--active,
 .nc-dashboard-shell--tauri .nc-dashboard-sidebar__item--active:hover,
 .nc-dashboard-shell--tauri .nc-dashboard-sidebar__item--active:focus-visible {
-  box-shadow: 0 9px 20px rgba(37, 99, 235, 0.14), inset 0 1px 0 rgba(255, 255, 255, 0.14);
+  box-shadow: inset 0 1px 0 rgba(255, 255, 255, 0.16);
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-impact {
-  padding: calc(var(--nc-impact-pad-y) + 2px) var(--nc-impact-pad-x) calc(var(--nc-impact-pad-y) + 7px);
+  border-color: rgba(190, 237, 250, 0.34);
+  border-radius: 22px;
+  margin-right: 10px;
+  margin-left: 10px;
+  box-shadow: 0 18px 36px rgba(28, 112, 210, 0.13), inset 0 1px 0 rgba(255, 255, 255, 0.22);
+  padding: calc(var(--nc-impact-pad-y) + 2px) var(--nc-impact-pad-x) calc(var(--nc-impact-pad-y) + 9px);
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-impact__heading {
-  gap: 10px;
+  gap: 8px;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-impact__icon {
-  width: 31px;
-  height: 31px;
-  flex-basis: 31px;
+  width: 28px;
+  height: 28px;
+  flex-basis: 28px;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-impact__content {
-  max-width: 172px;
+  max-width: 182px;
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-impact__title {
+  font-size: clamp(13px, calc(13.55px * var(--nc-sidebar-font-scale, var(--nc-font-scale))), 14.1px);
+  letter-spacing: -0.026em;
+  line-height: 1.12;
+  text-wrap: balance;
+  white-space: normal;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-impact__text {
-  max-width: 166px;
-  margin-top: 17px;
-  line-height: 1.54;
+  max-width: 162px;
+  margin-top: 19px;
+  line-height: 1.58;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-impact__plant {
-  right: -10px;
-  bottom: -17px;
+  right: -9px;
+  bottom: -20px;
+  opacity: 0.68;
 }
 
 .nc-dashboard-shell--tauri .nc-dashboard-impact__dots {
-  bottom: 18px;
+  bottom: 20px;
+  gap: 7px;
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-impact__dot {
+  width: 6px;
+  height: 4px;
+  background: rgba(255, 255, 255, 0.34);
+}
+
+.nc-dashboard-shell--tauri .nc-dashboard-impact__dot--active {
+  width: 24px;
+  background: rgba(255, 255, 255, 0.94);
 }
 
 @media (max-width: 1500px) {
   .nc-dashboard-shell--tauri .nc-dashboard-header__inner {
-    grid-template-columns: minmax(278px, 0.72fr) minmax(270px, 340px) max-content;
-    gap: 12px;
+    grid-template-columns: minmax(272px, 332px) minmax(250px, 320px) max-content;
+    column-gap: 12px;
+    row-gap: 12px;
   }
 }
 
 @media (max-width: 1420px) {
   .nc-dashboard-shell--tauri .nc-dashboard-header__inner {
-    grid-template-columns: minmax(260px, 0.68fr) minmax(230px, 300px) max-content;
-    gap: 10px;
+    grid-template-columns: minmax(238px, 292px) minmax(210px, 270px) max-content;
+    column-gap: 10px;
+    row-gap: 11px;
   }
 
   .nc-dashboard-shell--tauri .nc-dashboard-search {
